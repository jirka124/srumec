meta {
  name: Get Pending Events
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v1/events/get-pending
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
