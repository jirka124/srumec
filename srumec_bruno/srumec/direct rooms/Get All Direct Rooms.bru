meta {
  name: Get All Direct Rooms
  type: http
  seq: 1
}

post {
  url: http://localhost:8000/v1/chats/direct/get-all
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
}
